diff --git a/Cargo.lock b/Cargo.lock
index dba0c163..5783681c 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -3559,6 +3559,7 @@ dependencies = [
  "ironrdp-futures",
  "ironrdp-pdu",
  "ironrdp-propertyset",
+ "ironrdp-rail",
  "ironrdp-rdcleanpath",
  "ironrdp-rdpfile",
  "ironrdp-svc",
diff --git a/crates/ironrdp-capture-replay/src/routing.rs b/crates/ironrdp-capture-replay/src/routing.rs
index eb6915a6..b6527efa 100644
--- a/crates/ironrdp-capture-replay/src/routing.rs
+++ b/crates/ironrdp-capture-replay/src/routing.rs
@@ -222,6 +222,7 @@ impl ReplayRouter {
         }
 
         let active_stage = ActiveStageBuilder {
+            input_flags: ironrdp_pdu::rdp::capability_sets::InputFlags::empty(),
             static_channels: channels,
             user_channel_id: state.user_channel_id,
             io_channel_id: state.io_channel_id,
diff --git a/crates/ironrdp-client/src/rdp.rs b/crates/ironrdp-client/src/rdp.rs
index a67d49c9..4c82dc2a 100644
--- a/crates/ironrdp-client/src/rdp.rs
+++ b/crates/ironrdp-client/src/rdp.rs
@@ -3040,6 +3040,7 @@ async fn active_session(
     let activation_factory = connection_result.activation_factory;
 
     let mut active_stage = ActiveStageBuilder {
+        input_flags: connection_result.input_flags,
         static_channels: connection_result.static_channels,
         user_channel_id: connection_result.user_channel_id,
         io_channel_id: connection_result.io_channel_id,
@@ -4023,7 +4024,7 @@ async fn active_session(
                         if let ConnectionActivationState::Finalized {
                             desktop_size,
                             share_id,
-                            input_flags: _,
+                            input_flags,
                             enable_server_pointer,
                             pointer_software_rendering,
                             static_channel_chunk_size,
@@ -4047,6 +4048,7 @@ async fn active_session(
                             ) {
                                 return Err(ironrdp_session::general_err!("invalid static channel chunk size"));
                             }
+                            active_stage.set_input_flags(input_flags);
                             active_stage.set_window_support_level(window_support_level);
                             if let Some(monitor_layout) = connection_activation.monitor_layout()
                                 && !send_active_output_event(
@@ -5248,6 +5250,7 @@ mod tests {
                 .is_none()
         );
         let mut active_stage = ActiveStageBuilder {
+            input_flags: ironrdp_pdu::rdp::capability_sets::InputFlags::empty(),
             static_channels,
             user_channel_id: 1001,
             io_channel_id: 1003,
@@ -5285,6 +5288,7 @@ mod tests {
                 .is_none()
         );
         let mut active_stage = ActiveStageBuilder {
+            input_flags: ironrdp_pdu::rdp::capability_sets::InputFlags::empty(),
             static_channels,
             user_channel_id: 1001,
             io_channel_id: 1003,
diff --git a/crates/ironrdp-session/src/active_stage.rs b/crates/ironrdp-session/src/active_stage.rs
index f53e0aed..31539417 100644
--- a/crates/ironrdp-session/src/active_stage.rs
+++ b/crates/ironrdp-session/src/active_stage.rs
@@ -9,9 +9,10 @@ use ironrdp_egfx::client::GraphicsPipelineClient;
 use ironrdp_graphics::pointer::DecodedPointer;
 use ironrdp_pdu::gcc::{ChannelName, Monitor};
 use ironrdp_pdu::geometry::{ExclusiveRectangle, InclusiveRectangle, Rectangle as _};
-use ironrdp_pdu::input::fast_path::{FastPathInput, FastPathInputEvent};
+use ironrdp_pdu::input::fast_path::{FastPathInput, FastPathInputEvent, KeyboardFlags};
+use ironrdp_pdu::input::{InputEvent, InputEventPdu, ScanCodePdu, SyncPdu, UnicodePdu, scan_code, sync, unicode};
 use ironrdp_pdu::rdp::autodetect::AutoDetectRequest;
-use ironrdp_pdu::rdp::capability_sets::WindowSupportLevel;
+use ironrdp_pdu::rdp::capability_sets::{InputFlags, WindowSupportLevel};
 use ironrdp_pdu::rdp::client_info::CompressionType;
 use ironrdp_pdu::rdp::headers::ShareDataPdu;
 use ironrdp_pdu::rdp::multitransport::{MultitransportRequestPdu, MultitransportResponsePdu};
@@ -41,6 +42,7 @@ fn to_bulk_compression_type(compression_type: CompressionType) -> BulkCompressio
 }
 
 pub struct ActiveStage {
+    input_flags: InputFlags,
     x224_processor: x224::Processor,
     fast_path_processor: fast_path::Processor,
     /// Shared server-to-client compression history across all output transports.
@@ -56,6 +58,8 @@ pub struct ActiveStage {
 /// All fields are required; they are typically taken straight from `ironrdp-connector`’s
 /// `ConnectionResult` once the connection sequence is finalized.
 pub struct ActiveStageBuilder {
+    /// Input capabilities advertised by the server during activation.
+    pub input_flags: InputFlags,
     pub static_channels: StaticChannelSet,
     pub user_channel_id: u16,
     pub io_channel_id: u16,
@@ -72,6 +76,7 @@ pub struct ActiveStageBuilder {
 impl ActiveStageBuilder {
     pub fn build(self) -> ActiveStage {
         let Self {
+            input_flags,
             static_channels,
             user_channel_id,
             io_channel_id,
@@ -100,6 +105,7 @@ impl ActiveStageBuilder {
         .build();
 
         ActiveStage {
+            input_flags,
             x224_processor,
             fast_path_processor,
             bulk_decompressor: new_bulk_decompressor(compression_type),
@@ -116,6 +122,11 @@ fn new_bulk_decompressor(compression_type: Option<CompressionType>) -> Option<Bu
 }
 
 impl ActiveStage {
+    /// Updates the server input capabilities after reactivation.
+    pub fn set_input_flags(&mut self, input_flags: InputFlags) {
+        self.input_flags = input_flags;
+    }
+
     pub fn update_mouse_pos(&mut self, x: u16, y: u16) {
         self.fast_path_processor.update_mouse_pos(x, y);
     }
@@ -133,7 +144,8 @@ impl ActiveStage {
     }
 
     /// Encodes outgoing input events and modifies image if necessary (e.g for client-side pointer
-    /// rendering).
+    /// rendering). Uses fast-path only when advertised by the server; otherwise encodes
+    /// slow-path input on the global channel. QoE timestamps are omitted on slow-path.
     pub fn process_fastpath_input(
         &mut self,
         image: &mut DecodedImage,
@@ -149,9 +161,64 @@ impl ActiveStage {
         let mut output = Vec::with_capacity(events.len().div_ceil(FastPathInput::MAX_EVENTS) + 1);
 
         for event_chunk in events.chunks(FastPathInput::MAX_EVENTS) {
-            // PERF: unnecessary copy
-            let fastpath_input = FastPathInput::new(event_chunk.to_vec()).map_err(SessionError::decode)?;
-            let frame = ironrdp_core::encode_vec(&fastpath_input).map_err(SessionError::encode)?;
+            let frame = if self
+                .input_flags
+                .intersects(InputFlags::FASTPATH_INPUT | InputFlags::FASTPATH_INPUT_2)
+            {
+                // PERF: unnecessary copy
+                let fastpath_input = FastPathInput::new(event_chunk.to_vec()).map_err(SessionError::decode)?;
+                ironrdp_core::encode_vec(&fastpath_input).map_err(SessionError::encode)?
+            } else {
+                let events = event_chunk
+                    .iter()
+                    .filter_map(|event| {
+                        Some(match *event {
+                            FastPathInputEvent::KeyboardEvent(flags, code) => {
+                                let mut slow_flags = scan_code::KeyboardFlags::empty();
+                                slow_flags.set(
+                                    scan_code::KeyboardFlags::RELEASE,
+                                    flags.contains(KeyboardFlags::RELEASE),
+                                );
+                                slow_flags.set(
+                                    scan_code::KeyboardFlags::EXTENDED,
+                                    flags.contains(KeyboardFlags::EXTENDED),
+                                );
+                                slow_flags.set(
+                                    scan_code::KeyboardFlags::EXTENDED_1,
+                                    flags.contains(KeyboardFlags::EXTENDED1),
+                                );
+                                InputEvent::ScanCode(ScanCodePdu {
+                                    flags: slow_flags,
+                                    key_code: u16::from(code),
+                                })
+                            }
+                            FastPathInputEvent::UnicodeKeyboardEvent(flags, unicode_code) => {
+                                let mut slow_flags = unicode::KeyboardFlags::empty();
+                                slow_flags.set(unicode::KeyboardFlags::RELEASE, flags.contains(KeyboardFlags::RELEASE));
+                                InputEvent::Unicode(UnicodePdu {
+                                    flags: slow_flags,
+                                    unicode_code,
+                                })
+                            }
+                            FastPathInputEvent::MouseEvent(pdu) => InputEvent::Mouse(pdu),
+                            FastPathInputEvent::MouseEventEx(pdu) => InputEvent::MouseX(pdu),
+                            FastPathInputEvent::MouseEventRel(pdu) => InputEvent::MouseRel(pdu),
+                            FastPathInputEvent::SyncEvent(flags) => InputEvent::Sync(SyncPdu {
+                                flags: sync::SyncToggleFlags::from_bits_retain(u32::from(flags.bits())),
+                            }),
+                            // QoE timestamps have no slow-path input equivalent.
+                            FastPathInputEvent::QoeEvent(_) => return None,
+                        })
+                    })
+                    .collect::<Vec<_>>();
+                if events.is_empty() {
+                    continue;
+                }
+                let mut frame = WriteBuf::new();
+                self.x224_processor
+                    .encode_static(&mut frame, ShareDataPdu::Input(InputEventPdu(events)))?;
+                frame.into_inner()
+            };
             output.push(ActiveStageOutput::ResponseFrame(frame));
         }
 
@@ -1056,6 +1123,7 @@ mod tests {
     #[test]
     fn full_redraw_prefers_suppress_output_toggle_when_supported() {
         let stage = ActiveStageBuilder {
+            input_flags: InputFlags::FASTPATH_INPUT,
             static_channels: StaticChannelSet::new(),
             user_channel_id: 1001,
             io_channel_id: 1003,
@@ -1078,6 +1146,7 @@ mod tests {
     #[test]
     fn multitransport_response_encoder_is_exposed_through_active_stage() {
         let stage = ActiveStageBuilder {
+            input_flags: InputFlags::FASTPATH_INPUT,
             static_channels: StaticChannelSet::new(),
             user_channel_id: 1001,
             io_channel_id: 1003,
@@ -1104,6 +1173,7 @@ mod tests {
     #[test]
     fn fastpath_input_splits_at_event_limit() {
         let mut stage = ActiveStageBuilder {
+            input_flags: InputFlags::FASTPATH_INPUT,
             static_channels: StaticChannelSet::new(),
             user_channel_id: 1001,
             io_channel_id: 1003,
@@ -1149,6 +1219,7 @@ mod tests {
     #[test]
     fn fastpath_input_renders_the_last_mouse_position_in_a_batch() {
         let mut stage = ActiveStageBuilder {
+            input_flags: InputFlags::FASTPATH_INPUT,
             static_channels: StaticChannelSet::new(),
             user_channel_id: 1001,
             io_channel_id: 1003,
@@ -1527,6 +1598,7 @@ mod tests {
         );
 
         ActiveStageBuilder {
+            input_flags: InputFlags::FASTPATH_INPUT,
             static_channels,
             user_channel_id: 1001,
             io_channel_id: 1003,
diff --git a/crates/ironrdp-session/src/fast_path.rs b/crates/ironrdp-session/src/fast_path.rs
index 93b51181..2874a0fe 100644
--- a/crates/ironrdp-session/src/fast_path.rs
+++ b/crates/ironrdp-session/src/fast_path.rs
@@ -402,7 +402,11 @@ impl Processor {
                         // RDP6 bitmap streams are bottom-up, so reverse them while decoding to
                         // keep the framebuffer top-down. This matches FreeRDP's GDI frontend,
                         // which passes `vFlip = TRUE` to its planar bitmap decoder.
-                        Ok(()) => apply_bitmap(image.apply_rgb24(&buf, &update_rectangle, update.width, true))?,
+                        Ok(()) => {
+                            let rows =
+                                visible_bitmap_rows(&buf, update.width, update.height, update_rectangle.height(), 3);
+                            apply_bitmap(image.apply_rgb24(rows, &update_rectangle, update.width, true))?
+                        }
                         Err(err) => {
                             warn!("Invalid RDP6_BITMAP_STREAM: {err}");
                             None
@@ -414,24 +418,32 @@ impl Processor {
                     // 2.2.9.1.1.3.1.2.4).
                     debug!(bpp = update.bits_per_pixel, "Non-32 bpp compressed RLE_BITMAP_STREAM",);
 
-                    match ironrdp_graphics::rle::decompress(
+                    let decoded = ironrdp_graphics::rle::decompress(
                         update.bitmap_data,
                         &mut buf,
                         usize::from(update.width),
                         usize::from(update.height),
                         usize::from(update.bits_per_pixel),
-                    ) {
+                    );
+                    let rows = visible_bitmap_rows(
+                        &buf,
+                        update.width,
+                        update.height,
+                        update_rectangle.height(),
+                        usize::from(update.bits_per_pixel).div_ceil(8),
+                    );
+                    match decoded {
                         Ok(RlePixelFormat::Rgb16) => {
-                            apply_bitmap(image.apply_rgb16_bitmap(&buf, &update_rectangle, update.width))?
+                            apply_bitmap(image.apply_rgb16_bitmap(rows, &update_rectangle, update.width))?
                         }
                         Ok(RlePixelFormat::Rgb15) => {
-                            apply_bitmap(image.apply_rgb15_bitmap(&buf, &update_rectangle, update.width))?
+                            apply_bitmap(image.apply_rgb15_bitmap(rows, &update_rectangle, update.width))?
                         }
                         Ok(RlePixelFormat::Rgb24) => {
-                            apply_bitmap(image.apply_bgr24_bitmap(&buf, &update_rectangle, update.width))?
+                            apply_bitmap(image.apply_bgr24_bitmap(rows, &update_rectangle, update.width))?
                         }
                         Ok(RlePixelFormat::Rgb8) => apply_bitmap(image.apply_rgb8_with_palette(
-                            &buf,
+                            rows,
                             &update_rectangle,
                             self.palette.colors(),
                             update.width,
@@ -468,18 +480,25 @@ impl Processor {
                         buf.extend_from_slice(&row[..row_bytes]);
                     }
 
+                    let rows = visible_bitmap_rows(
+                        &buf,
+                        update.width,
+                        update.height,
+                        update_rectangle.height(),
+                        bytes_per_pixel,
+                    );
                     match update.bits_per_pixel {
                         8 => apply_bitmap(image.apply_rgb8_with_palette(
-                            &buf,
+                            rows,
                             &update_rectangle,
                             self.palette.colors(),
                             update.width,
                         ))?,
-                        15 => apply_bitmap(image.apply_rgb15_bitmap(&buf, &update_rectangle, update.width))?,
-                        16 => apply_bitmap(image.apply_rgb16_bitmap(&buf, &update_rectangle, update.width))?,
-                        24 => apply_bitmap(image.apply_bgr24_bitmap(&buf, &update_rectangle, update.width))?,
+                        15 => apply_bitmap(image.apply_rgb15_bitmap(rows, &update_rectangle, update.width))?,
+                        16 => apply_bitmap(image.apply_rgb16_bitmap(rows, &update_rectangle, update.width))?,
+                        24 => apply_bitmap(image.apply_bgr24_bitmap(rows, &update_rectangle, update.width))?,
                         32 => apply_bitmap(image.apply_rgb32_bitmap(
-                            &buf,
+                            rows,
                             PixelFormat::BgrX32,
                             &update_rectangle,
                             update.width,
@@ -490,24 +509,25 @@ impl Processor {
                         }
                     }
                 } else {
+                    let rows = visible_bitmap_rows(
+                        update.bitmap_data,
+                        update.width,
+                        update.height,
+                        update_rectangle.height(),
+                        bytes_per_pixel,
+                    );
                     match update.bits_per_pixel {
                         8 => apply_bitmap(image.apply_rgb8_with_palette(
-                            update.bitmap_data,
+                            rows,
                             &update_rectangle,
                             self.palette.colors(),
                             update.width,
                         ))?,
-                        15 => {
-                            apply_bitmap(image.apply_rgb15_bitmap(update.bitmap_data, &update_rectangle, update.width))?
-                        }
-                        16 => {
-                            apply_bitmap(image.apply_rgb16_bitmap(update.bitmap_data, &update_rectangle, update.width))?
-                        }
-                        24 => {
-                            apply_bitmap(image.apply_bgr24_bitmap(update.bitmap_data, &update_rectangle, update.width))?
-                        }
+                        15 => apply_bitmap(image.apply_rgb15_bitmap(rows, &update_rectangle, update.width))?,
+                        16 => apply_bitmap(image.apply_rgb16_bitmap(rows, &update_rectangle, update.width))?,
+                        24 => apply_bitmap(image.apply_bgr24_bitmap(rows, &update_rectangle, update.width))?,
                         32 => apply_bitmap(image.apply_rgb32_bitmap(
-                            update.bitmap_data,
+                            rows,
                             PixelFormat::BgrX32,
                             &update_rectangle,
                             update.width,
@@ -530,7 +550,27 @@ impl Processor {
             }
         }
 
-        Ok(vec![update_kind])
+        return Ok(vec![update_kind]);
+
+        fn visible_bitmap_rows(
+            data: &[u8],
+            width: u16,
+            height: u16,
+            visible_height: u16,
+            bytes_per_pixel: usize,
+        ) -> &[u8] {
+            let stride = usize::from(width) * bytes_per_pixel;
+            let Some(expected_length) = stride.checked_mul(usize::from(height)) else {
+                return &[];
+            };
+            if data.len() != expected_length {
+                // Preserve the apply methods' invalid-source-length recovery path.
+                return &[];
+            }
+            // Bottom-up data ends with the visual top row. Discard visual bottom
+            // rows (including height padding), keeping the full source stride.
+            &data[usize::from(height - visible_height) * stride..]
+        }
     }
 
     /// Process a pointer update, shared between fast-path and slow-path pipelines.
diff --git a/crates/ironrdp-session/src/image.rs b/crates/ironrdp-session/src/image.rs
index fb19afbe..67c6c086 100644
--- a/crates/ironrdp-session/src/image.rs
+++ b/crates/ironrdp-session/src/image.rs
@@ -275,8 +275,24 @@ impl DecodedImage {
         width: u16,
         height: u16,
     ) -> Option<InclusiveRectangle> {
-        (self.rect_fits(update_rectangle) && width >= update_rectangle.width() && update_rectangle.height() == height)
-            .then_some(update_rectangle.clone())
+        // Validate the declared destination before clipping: clipping must not hide
+        // insufficient source dimensions or an inverted rectangle.
+        if update_rectangle.left > update_rectangle.right
+            || update_rectangle.top > update_rectangle.bottom
+            || update_rectangle.left >= self.width
+            || update_rectangle.top >= self.height
+            || u32::from(width) < u32::from(update_rectangle.right) - u32::from(update_rectangle.left) + 1
+            || u32::from(height) < u32::from(update_rectangle.bottom) - u32::from(update_rectangle.top) + 1
+        {
+            return None;
+        }
+
+        Some(InclusiveRectangle {
+            left: update_rectangle.left,
+            top: update_rectangle.top,
+            right: update_rectangle.right.min(self.width - 1),
+            bottom: update_rectangle.bottom.min(self.height - 1),
+        })
     }
 
     fn apply_pointer_layer(&mut self, layer: PointerLayer) -> SessionResult<Option<InclusiveRectangle>> {
diff --git a/crates/ironrdp-testsuite-core/tests/session/active_stage.rs b/crates/ironrdp-testsuite-core/tests/session/active_stage.rs
index bbf84400..66cadd1c 100644
--- a/crates/ironrdp-testsuite-core/tests/session/active_stage.rs
+++ b/crates/ironrdp-testsuite-core/tests/session/active_stage.rs
@@ -307,6 +307,7 @@ fn active_stage_with_active_egfx() -> ActiveStage {
     );
 
     ActiveStageBuilder {
+        input_flags: ironrdp_pdu::rdp::capability_sets::InputFlags::empty(),
         static_channels,
         user_channel_id: USER_CHANNEL_ID,
         io_channel_id: IO_CHANNEL_ID,
@@ -427,3 +428,97 @@ fn egfx_reset_resizes_the_image_before_compositing_the_same_payload() {
         "the filled pixel must be present at its new-output coordinates"
     );
 }
+
+#[test]
+fn input_transport_follows_activation_flags() {
+    use ironrdp_core::decode;
+    use ironrdp_pdu::input::fast_path::{FastPathInput, FastPathInputEvent, KeyboardFlags, SynchronizeFlags};
+    use ironrdp_pdu::input::{InputEvent, MousePdu, MouseXPdu, mouse, mouse_x};
+    use ironrdp_pdu::mcs::SendDataRequest;
+    use ironrdp_pdu::rdp::capability_sets::InputFlags;
+    use ironrdp_pdu::rdp::headers::{ShareControlHeader, ShareControlPdu, ShareDataPdu};
+
+    let mut stage = ActiveStageBuilder {
+        input_flags: InputFlags::empty(),
+        static_channels: StaticChannelSet::new(),
+        user_channel_id: 1001,
+        io_channel_id: 1003,
+        message_channel_id: None,
+        share_id: 1,
+        compression_type: None,
+        enable_server_pointer: false,
+        pointer_software_rendering: false,
+    }
+    .build();
+    let mut image = DecodedImage::new(PixelFormat::RgbA32, 1, 1);
+    let events = [
+        FastPathInputEvent::KeyboardEvent(KeyboardFlags::empty(), 0x1e),
+        FastPathInputEvent::KeyboardEvent(
+            KeyboardFlags::RELEASE | KeyboardFlags::EXTENDED | KeyboardFlags::EXTENDED1,
+            0x1d,
+        ),
+        FastPathInputEvent::UnicodeKeyboardEvent(KeyboardFlags::RELEASE, 0x20ac),
+        FastPathInputEvent::MouseEvent(MousePdu {
+            flags: mouse::PointerFlags::MOVE,
+            number_of_wheel_rotation_units: 0,
+            x_position: 42,
+            y_position: 24,
+        }),
+        FastPathInputEvent::MouseEventEx(MouseXPdu {
+            flags: mouse_x::PointerXFlags::empty(),
+            x_position: 12,
+            y_position: 34,
+        }),
+        FastPathInputEvent::SyncEvent(SynchronizeFlags::CAPS_LOCK | SynchronizeFlags::NUM_LOCK),
+    ];
+    // Switch both ways to cover capability changes during reactivation, including INPUT2 alone.
+    for flags in [
+        InputFlags::empty(),
+        InputFlags::FASTPATH_INPUT,
+        InputFlags::FASTPATH_INPUT_2,
+        InputFlags::SCANCODES,
+    ] {
+        stage.set_input_flags(flags);
+        let output = stage.process_fastpath_input(&mut image, &events).unwrap();
+        let [ActiveStageOutput::ResponseFrame(frame)] = output.as_slice() else {
+            panic!("expected one input frame")
+        };
+        if flags.intersects(InputFlags::FASTPATH_INPUT | InputFlags::FASTPATH_INPUT_2) {
+            let pdu: FastPathInput = decode(frame).unwrap();
+            assert_eq!(pdu.input_events(), events);
+        } else {
+            let request: X224<SendDataRequest<'_>> = decode(frame).unwrap();
+            assert_eq!(request.0.initiator_id, 1001);
+            assert_eq!(request.0.channel_id, 1003);
+            let header: ShareControlHeader = decode(&request.0.user_data).unwrap();
+            assert_eq!(header.share_id, 1);
+            let ShareControlPdu::Data(data) = header.share_control_pdu else {
+                panic!("expected share data")
+            };
+            let ShareDataPdu::Input(input) = data.share_data_pdu else {
+                panic!("expected input")
+            };
+            assert_eq!(input.0.len(), events.len());
+            assert!(matches!(&input.0[0], InputEvent::ScanCode(pdu) if pdu.flags.bits() == 0 && pdu.key_code == 0x1e));
+            assert!(
+                matches!(&input.0[1], InputEvent::ScanCode(pdu) if pdu.flags.bits() == 0x8300 && pdu.key_code == 0x1d)
+            );
+            assert!(
+                matches!(&input.0[2], InputEvent::Unicode(pdu) if pdu.flags.bits() == 0x8000 && pdu.unicode_code == 0x20ac)
+            );
+            assert!(matches!(&input.0[3], InputEvent::Mouse(pdu) if pdu.x_position == 42 && pdu.y_position == 24));
+            assert!(matches!(&input.0[4], InputEvent::MouseX(pdu) if pdu.x_position == 12 && pdu.y_position == 34));
+            assert!(matches!(&input.0[5], InputEvent::Sync(pdu) if pdu.flags.bits() == 6));
+        }
+    }
+    stage.set_input_flags(InputFlags::empty());
+    assert!(stage.process_fastpath_input(&mut image, &[]).unwrap().is_empty());
+    assert!(
+        stage
+            .process_fastpath_input(&mut image, &[FastPathInputEvent::QoeEvent(123)])
+            .unwrap()
+            .is_empty()
+    );
+    let output = stage.process_fastpath_input(&mut image, &vec![events[0]; 256]).unwrap();
+    assert_eq!(output.len(), 2);
+}
diff --git a/crates/ironrdp-testsuite-core/tests/session/fast_path.rs b/crates/ironrdp-testsuite-core/tests/session/fast_path.rs
index a13b81e7..f830bdf5 100644
--- a/crates/ironrdp-testsuite-core/tests/session/fast_path.rs
+++ b/crates/ironrdp-testsuite-core/tests/session/fast_path.rs
@@ -216,3 +216,110 @@ fn fragmented_compressed_fastpath_update_decompresses_before_reassembly() {
     );
     assert_eq!(image.data(), expected.data());
 }
+
+#[test]
+fn bitmap_updates_clip_destination_and_source_height_without_flipping_visible_rows() {
+    // Normal destination, bottom/right clipping, extra source height, and both.
+    for (right, bottom, height) in [(2, 2, 2), (3, 3, 3), (2, 2, 4), (3, 3, 4)] {
+        for (bits_per_pixel, compressed) in [(24, false), (32, false), (24, true)] {
+            let mut pixels = Vec::new();
+            for y in (0..height).rev() {
+                for x in 0..3u8 {
+                    pixels.extend_from_slice(&[x, y, 10 + y * 3 + x]);
+                    if bits_per_pixel == 32 {
+                        pixels.push(255);
+                    }
+                }
+                if bits_per_pixel == 24 && !compressed {
+                    // Three pixels occupy nine bytes, padded to twelve on the wire.
+                    pixels.extend_from_slice(&[0; 3]);
+                }
+            }
+            if compressed {
+                // Interleaved RLE REGULAR_COLOR_IMAGE: literal bottom-up pixels.
+                pixels.insert(0, 0x80 | (3 * height));
+            }
+            let mut image = DecodedImage::new(PixelFormat::RgbA32, 3, 3);
+            let updates = processor()
+                .process_bitmap_update(
+                    &mut image,
+                    BitmapUpdateData {
+                        rectangles: vec![BitmapData {
+                            rectangle: InclusiveRectangle {
+                                left: 1,
+                                top: 1,
+                                right,
+                                bottom,
+                            },
+                            width: 3,
+                            height: u16::from(height),
+                            bits_per_pixel,
+                            compression_flags: if compressed {
+                                Compression::BITMAP_COMPRESSION
+                            } else {
+                                Compression::empty()
+                            },
+                            compressed_data_header: None,
+                            bitmap_data: &pixels,
+                        }],
+                    },
+                )
+                .expect("apply clipped bitmap");
+            assert!(matches!(
+                updates.as_slice(),
+                [ironrdp_session::fast_path::UpdateKind::Region(InclusiveRectangle {
+                    left: 1,
+                    top: 1,
+                    right: 2,
+                    bottom: 2,
+                })]
+            ));
+            let mut expected = vec![0; 3 * 3 * 4];
+            expected[16..24].copy_from_slice(&[10, 0, 0, 255, 11, 0, 1, 255]);
+            expected[28..36].copy_from_slice(&[13, 1, 0, 255, 14, 1, 1, 255]);
+            assert_eq!(
+                image.data(),
+                expected,
+                "destination ({right}, {bottom}), height {height}, depth {bits_per_pixel}, compressed {compressed}"
+            );
+        }
+    }
+}
+
+#[test]
+fn bitmap_updates_reject_outside_destinations_and_insufficient_sources() {
+    for (left, top, right, bottom, width, height) in [
+        (3, 0, 4, 1, 2, 2), // wholly beyond the right edge
+        (0, 3, 1, 4, 2, 2), // wholly below the bottom edge
+        (1, 1, 3, 2, 2, 2), // insufficient width before clipping
+        (1, 1, 2, 3, 2, 2), // insufficient height before clipping
+    ] {
+        let mut image = DecodedImage::new(PixelFormat::RgbA32, 3, 3);
+        let updates = processor()
+            .process_bitmap_update(
+                &mut image,
+                BitmapUpdateData {
+                    rectangles: vec![BitmapData {
+                        rectangle: InclusiveRectangle {
+                            left,
+                            top,
+                            right,
+                            bottom,
+                        },
+                        width,
+                        height,
+                        bits_per_pixel: 32,
+                        compression_flags: Compression::empty(),
+                        compressed_data_header: None,
+                        bitmap_data: &[255; 16],
+                    }],
+                },
+            )
+            .expect("ignore invalid bitmap destination");
+        assert!(matches!(
+            updates.as_slice(),
+            [ironrdp_session::fast_path::UpdateKind::None]
+        ));
+        assert_eq!(image.data(), &[0; 36]);
+    }
+}
diff --git a/crates/ironrdp-testsuite-core/tests/svc.rs b/crates/ironrdp-testsuite-core/tests/svc.rs
index d7ce9517..158b14e0 100644
--- a/crates/ironrdp-testsuite-core/tests/svc.rs
+++ b/crates/ironrdp-testsuite-core/tests/svc.rs
@@ -278,6 +278,7 @@ fn reactivated_session_applies_the_static_channel_chunk_size() {
     channels.attach_channel_id_by_key(key, 1005);
 
     let mut active_stage = ActiveStageBuilder {
+        input_flags: ironrdp_pdu::rdp::capability_sets::InputFlags::empty(),
         static_channels: channels,
         user_channel_id: 1002,
         io_channel_id: 1003,
diff --git a/crates/ironrdp-testsuite-extra/tests/e2e.rs b/crates/ironrdp-testsuite-extra/tests/e2e.rs
index 8e2d7055..171beb9e 100644
--- a/crates/ironrdp-testsuite-extra/tests/e2e.rs
+++ b/crates/ironrdp-testsuite-extra/tests/e2e.rs
@@ -163,6 +163,7 @@ async fn test_deactivation_reactivation() {
 #[test]
 fn test_reactivation_preserves_bulk_decompression_history() {
     let mut stage = ActiveStageBuilder {
+        input_flags: ironrdp_pdu::rdp::capability_sets::InputFlags::empty(),
         static_channels: StaticChannelSet::new(),
         user_channel_id: 1001,
         io_channel_id: 1003,
@@ -986,6 +987,7 @@ async fn client_server_with_connector<F, Fut, C>(
                 // Deactivation-Reactivation Sequence.
                 let activation_factory = connection_result.activation_factory;
                 let active_stage = ActiveStageBuilder {
+                    input_flags: connection_result.input_flags,
                     static_channels: connection_result.static_channels,
                     user_channel_id: connection_result.user_channel_id,
                     io_channel_id: connection_result.io_channel_id,
diff --git a/crates/ironrdp-web/Cargo.toml b/crates/ironrdp-web/Cargo.toml
index 8c470c09..28e2f903 100644
--- a/crates/ironrdp-web/Cargo.toml
+++ b/crates/ironrdp-web/Cargo.toml
@@ -48,6 +48,7 @@ ironrdp-vmconnect.path = "../ironrdp-vmconnect"
 ironrdp-propertyset.path = "../ironrdp-propertyset"
 ironrdp-rdpfile.path = "../ironrdp-rdpfile"
 ironrdp-svc.path = "../ironrdp-svc"
+ironrdp-rail.path = "../ironrdp-rail"
 iron-remote-desktop.path = "../iron-remote-desktop"
 
 # WASM
diff --git a/crates/ironrdp-web/src/lib.rs b/crates/ironrdp-web/src/lib.rs
index 664e42e6..a77ec712 100644
--- a/crates/ironrdp-web/src/lib.rs
+++ b/crates/ironrdp-web/src/lib.rs
@@ -22,6 +22,7 @@ mod image;
 mod input;
 mod network_client;
 mod printer;
+mod rail;
 mod rdp_file;
 mod session;
 
diff --git a/crates/ironrdp-web/src/rail.rs b/crates/ironrdp-web/src/rail.rs
new file mode 100644
index 00000000..f0ce1d32
--- /dev/null
+++ b/crates/ironrdp-web/src/rail.rs
@@ -0,0 +1,468 @@
+// Copiado de ironrdp-client/src/rail.rs (processador do canal estático RAIL, independente de runtime)
+// para o cliente web poder abrir RemoteApps.
+#![allow(dead_code)]
+use std::collections::VecDeque;
+
+use ironrdp_core::{AsAny, decode};
+use ironrdp_pdu::gcc::{ChannelName, ChannelOptions};
+use ironrdp_pdu::{PduError, PduErrorExt as _, PduErrorKind, PduResult, decode_err};
+use ironrdp_rail::pdu::{
+    ActivatePdu, ClientStatusPdu, ClientSystemParameter, ClientSystemParametersPdu, CloakPdu, CompartmentInfoPdu,
+    ExecutePdu, ExecuteResultPdu, GetApplicationIdRequestPdu, GetApplicationIdResponseExPdu,
+    GetApplicationIdResponsePdu, HandshakeExPdu, HandshakePdu, LanguageBarInfoPdu, NotifyEventPdu,
+    PowerDisplayRequestPdu, RailPdu, ServerSystemParametersPdu, SystemCommandPdu, SystemMenuPdu,
+    SystemParameterRectangle, ZOrderSyncPdu,
+};
+use ironrdp_svc::{ChannelFlags, SvcClientProcessor, SvcMessage, SvcProcessor, SvcProcessorMessages};
+
+/// Client-originated RAIL events used by the initial RemoteApp launch flow.
+///
+/// Other client-valid RAIL PDUs, including window move and IME controls, are
+/// not yet exposed through this host-neutral boundary.
+#[derive(Debug, Clone, Copy, PartialEq, Eq)]
+pub enum RailInputEvent {
+    Activate(ActivatePdu),
+    SystemMenu(SystemMenuPdu),
+    SystemCommand(SystemCommandPdu),
+    NotifyEvent(NotifyEventPdu),
+    GetApplicationId(GetApplicationIdRequestPdu),
+}
+
+impl From<RailInputEvent> for RailPdu {
+    fn from(event: RailInputEvent) -> Self {
+        match event {
+            RailInputEvent::Activate(pdu) => Self::Activate(pdu),
+            RailInputEvent::SystemMenu(pdu) => Self::SystemMenu(pdu),
+            RailInputEvent::SystemCommand(pdu) => Self::SystemCommand(pdu),
+            RailInputEvent::NotifyEvent(pdu) => Self::NotifyEvent(pdu),
+            RailInputEvent::GetApplicationId(pdu) => Self::GetApplicationIdRequest(pdu),
+        }
+    }
+}
+
+/// Portable server-originated RAIL controls.
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub enum RailControlEvent {
+    SystemParameters(ServerSystemParametersPdu),
+    LanguageBar(LanguageBarInfoPdu),
+    Compartment(CompartmentInfoPdu),
+    ZOrderSync(ZOrderSyncPdu),
+    Cloak(CloakPdu),
+    PowerDisplayRequest(PowerDisplayRequestPdu),
+}
+
+/// Events emitted by a client-side RAIL channel processor.
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub enum RailEvent {
+    Handshake {
+        handshake_ex_flags: Option<u32>,
+        initialization_message_count: usize,
+        queued_execute_count: usize,
+    },
+    DesktopSynchronized {
+        released_execute_count: usize,
+    },
+    PostHandshakeQueueReleased {
+        released_execute_count: usize,
+    },
+    ExecuteResult(ExecuteResultPdu),
+    ApplicationId {
+        window_id: u32,
+        application_id: String,
+        process_id: Option<u32>,
+        process_image_name: Option<String>,
+    },
+    Control(RailControlEvent),
+}
+
+/// PDUs emitted by [`RailClient`].
+pub type RailSvcMessages = SvcProcessorMessages<RailClient>;
+
+#[derive(Debug, Copy, Clone, PartialEq, Eq)]
+enum RailState {
+    WaitingForHandshake,
+    Established,
+}
+
+#[derive(Debug)]
+enum QueuedRailPdu {
+    Execute(ExecutePdu),
+    Input(RailInputEvent),
+}
+
+#[derive(Debug, PartialEq, Eq)]
+struct PendingExecute {
+    flags: u16,
+    executable: String,
+}
+
+/// Client-side processor for the RAIL static virtual channel.
+#[derive(Debug)]
+pub struct RailClient {
+    build_number: u32,
+    desktop_width: u16,
+    desktop_height: u16,
+    client_status_flags: u32,
+    state: RailState,
+    desktop_synchronized: bool,
+    queued_pdus: VecDeque<QueuedRailPdu>,
+    pending_executes: VecDeque<PendingExecute>,
+    events: VecDeque<RailEvent>,
+}
+
+impl RailClient {
+    const CHANNEL_NAME: ChannelName = ChannelName::from_static(b"rail\0\0\0\0");
+    const MAX_QUEUED_PDUS: usize = 128;
+    const MAX_PENDING_EXECUTES: usize = 64;
+    const MAX_EVENTS: usize = 256;
+
+    /// Creates a RAIL static-channel processor for the client's build and desktop size.
+    pub fn new(build_number: u32, desktop_width: u16, desktop_height: u16) -> Self {
+        Self {
+            build_number,
+            desktop_width,
+            desktop_height,
+            client_status_flags: ClientStatusPdu::Z_ORDER_SYNC
+                | ClientStatusPdu::POWER_DISPLAY_REQUEST
+                | ClientStatusPdu::BIDIRECTIONAL_CLOAK,
+            state: RailState::WaitingForHandshake,
+            desktop_synchronized: false,
+            queued_pdus: VecDeque::new(),
+            pending_executes: VecDeque::new(),
+            events: VecDeque::new(),
+        }
+    }
+
+    /// Overrides the RAIL Client Status flags advertised during initialization.
+    #[must_use]
+    pub fn with_client_status_flags(mut self, flags: u32) -> Self {
+        self.client_status_flags = flags;
+        self
+    }
+
+    /// Queues a RemoteApp launch until initialization and desktop synchronization complete.
+    pub fn queue_execute(&mut self, execute: ExecutePdu) -> PduResult<RailSvcMessages> {
+        self.queue_pdu(QueuedRailPdu::Execute(execute))
+    }
+
+    /// Queues a client-originated RAIL input event.
+    pub fn queue_input(&mut self, event: RailInputEvent) -> PduResult<RailSvcMessages> {
+        self.queue_pdu(QueuedRailPdu::Input(event))
+    }
+
+    /// Releases queued RAIL input after the server completes desktop synchronization.
+    pub fn complete_desktop_synchronization(&mut self) -> PduResult<RailSvcMessages> {
+        if self.desktop_synchronized {
+            return Ok(Vec::new().into());
+        }
+
+        self.desktop_synchronized = true;
+        if !self.is_established() {
+            return Ok(Vec::new().into());
+        }
+
+        let (messages, released_execute_count) = self.drain_queued_pdus()?;
+        self.push_event(RailEvent::DesktopSynchronized { released_execute_count })?;
+        Ok(messages.into())
+    }
+
+    /// Releases queued input after initialization when desktop synchronization is not observable.
+    pub fn release_queued_after_handshake(&mut self) -> PduResult<RailSvcMessages> {
+        if self.desktop_synchronized || !self.is_established() {
+            return Ok(Vec::new().into());
+        }
+
+        self.desktop_synchronized = true;
+        let (messages, released_execute_count) = self.drain_queued_pdus()?;
+        self.push_event(RailEvent::PostHandshakeQueueReleased { released_execute_count })?;
+        Ok(messages.into())
+    }
+
+    /// Updates the RAIL work area after the client desktop size changes.
+    pub fn update_desktop_size(&mut self, desktop_width: u16, desktop_height: u16) -> RailSvcMessages {
+        self.desktop_width = desktop_width;
+        self.desktop_height = desktop_height;
+        if !self.is_established() {
+            return Vec::new().into();
+        }
+
+        self.desktop_system_parameter_messages().into()
+    }
+
+    /// Returns and clears server events accumulated while processing channel PDUs.
+    pub fn take_events(&mut self) -> Vec<RailEvent> {
+        self.events.drain(..).collect()
+    }
+
+    const fn is_established(&self) -> bool {
+        matches!(self.state, RailState::Established)
+    }
+
+    fn queue_pdu(&mut self, pdu: QueuedRailPdu) -> PduResult<RailSvcMessages> {
+        Self::validate_queued_pdu(&pdu)?;
+        if !self.is_established() || !self.desktop_synchronized {
+            if self.queued_pdus.len() >= Self::MAX_QUEUED_PDUS {
+                return Err(Self::resource_limit_error("queued RAIL PDUs"));
+            }
+            if matches!(&pdu, QueuedRailPdu::Execute(_))
+                && self.pending_executes.len() + self.queued_execute_count() >= Self::MAX_PENDING_EXECUTES
+            {
+                return Err(Self::resource_limit_error("RAIL execute requests"));
+            }
+            self.queued_pdus.push_back(pdu);
+            return Ok(Vec::new().into());
+        }
+
+        Ok(self.send_pdu(pdu)?.into())
+    }
+
+    fn initialization_messages(&self) -> Vec<SvcMessage> {
+        let mut messages = vec![
+            Self::service_message(RailPdu::Handshake(HandshakePdu {
+                build_number: self.build_number,
+            })),
+            Self::service_message(RailPdu::ClientStatus(ClientStatusPdu {
+                flags: self.client_status_flags,
+            })),
+        ];
+        messages.extend(self.desktop_system_parameter_messages());
+        messages.extend([
+            Self::service_message(RailPdu::ClientSystemParameters(ClientSystemParametersPdu {
+                parameter: ClientSystemParameter::FullWindowDrag(false),
+            })),
+            Self::service_message(RailPdu::ClientSystemParameters(ClientSystemParametersPdu {
+                parameter: ClientSystemParameter::KeyboardCues(false),
+            })),
+            Self::service_message(RailPdu::ClientSystemParameters(ClientSystemParametersPdu {
+                parameter: ClientSystemParameter::KeyboardPreference(false),
+            })),
+            Self::service_message(RailPdu::ClientSystemParameters(ClientSystemParametersPdu {
+                parameter: ClientSystemParameter::MouseButtonSwap(false),
+            })),
+        ]);
+        messages
+    }
+
+    fn desktop_system_parameter_messages(&self) -> Vec<SvcMessage> {
+        let screen = SystemParameterRectangle {
+            left: 0,
+            top: 0,
+            right: self.desktop_width,
+            bottom: self.desktop_height,
+        };
+        [
+            ClientSystemParameter::DisplayChange(screen),
+            ClientSystemParameter::WorkArea(screen),
+        ]
+        .into_iter()
+        .map(|parameter| {
+            Self::service_message(RailPdu::ClientSystemParameters(ClientSystemParametersPdu { parameter }))
+        })
+        .collect()
+    }
+
+    fn queued_execute_count(&self) -> usize {
+        self.queued_pdus
+            .iter()
+            .filter(|pdu| matches!(pdu, QueuedRailPdu::Execute(_)))
+            .count()
+    }
+
+    fn finish_handshake(&mut self, handshake_ex_flags: Option<u32>) -> PduResult<Vec<SvcMessage>> {
+        self.state = RailState::Established;
+        let queued_execute_count = self.queued_execute_count();
+        let mut messages = self.initialization_messages();
+        self.push_event(RailEvent::Handshake {
+            handshake_ex_flags,
+            initialization_message_count: messages.len(),
+            queued_execute_count,
+        })?;
+        if self.desktop_synchronized {
+            let (queued_messages, released_execute_count) = self.drain_queued_pdus()?;
+            messages.extend(queued_messages);
+            self.push_event(RailEvent::DesktopSynchronized { released_execute_count })?;
+        }
+        Ok(messages)
+    }
+
+    fn send_pdu(&mut self, pdu: QueuedRailPdu) -> PduResult<Vec<SvcMessage>> {
+        match pdu {
+            QueuedRailPdu::Execute(execute) => {
+                if self.pending_executes.len() >= Self::MAX_PENDING_EXECUTES {
+                    return Err(Self::resource_limit_error("pending RAIL execute requests"));
+                }
+                self.pending_executes.push_back(PendingExecute {
+                    flags: execute.flags,
+                    executable: execute.executable.clone(),
+                });
+                Ok(vec![Self::service_message(RailPdu::Execute(execute))])
+            }
+            QueuedRailPdu::Input(event) => Ok(vec![Self::service_message(RailPdu::from(event))]),
+        }
+    }
+
+    fn drain_queued_pdus(&mut self) -> PduResult<(Vec<SvcMessage>, usize)> {
+        let mut messages = Vec::new();
+        let mut released_execute_count = 0;
+        while let Some(pdu) = self.queued_pdus.pop_front() {
+            if matches!(pdu, QueuedRailPdu::Execute(_)) {
+                released_execute_count += 1;
+            }
+            messages.extend(self.send_pdu(pdu)?);
+        }
+        Ok((messages, released_execute_count))
+    }
+
+    fn push_event(&mut self, event: RailEvent) -> PduResult<()> {
+        if self.events.len() >= Self::MAX_EVENTS {
+            return Err(Self::resource_limit_error("queued RAIL events"));
+        }
+        self.events.push_back(event);
+        Ok(())
+    }
+
+    fn validate_queued_pdu(pdu: &QueuedRailPdu) -> PduResult<()> {
+        let pdu = match pdu {
+            QueuedRailPdu::Execute(execute) => RailPdu::Execute(execute.clone()),
+            QueuedRailPdu::Input(event) => RailPdu::from(*event),
+        };
+        pdu.validate()
+            .map_err(|error| PduError::encode("RAIL client request", error))
+    }
+
+    fn service_message(pdu: RailPdu) -> SvcMessage {
+        SvcMessage::from(pdu).with_flags(ChannelFlags::SHOW_PROTOCOL)
+    }
+
+    fn resource_limit_error(description: &'static str) -> PduError {
+        PduError::new("RAIL", PduErrorKind::Other { description })
+    }
+}
+
+impl SvcClientProcessor for RailClient {}
+
+impl AsAny for RailClient {
+    fn as_any(&self) -> &dyn core::any::Any {
+        self
+    }
+
+    fn as_any_mut(&mut self) -> &mut dyn core::any::Any {
+        self
+    }
+}
+
+impl SvcProcessor for RailClient {
+    fn channel_name(&self) -> ChannelName {
+        Self::CHANNEL_NAME
+    }
+
+    fn channel_options(&self) -> ChannelOptions {
+        ChannelOptions::INITIALIZED | ChannelOptions::SHOW_PROTOCOL
+    }
+
+    fn process(&mut self, payload: &[u8]) -> PduResult<Vec<SvcMessage>> {
+        let pdu = decode::<RailPdu>(payload).map_err(|error| decode_err!(error))?;
+        if self.is_established() && !pdu.is_server_to_client() {
+            return Err(PduError::new(
+                "RAIL",
+                PduErrorKind::Other {
+                    description: "received client-originated RAIL PDU from server",
+                },
+            ));
+        }
+
+        match (self.state, pdu) {
+            (RailState::WaitingForHandshake, RailPdu::Handshake(HandshakePdu { .. })) => self.finish_handshake(None),
+            (RailState::WaitingForHandshake, RailPdu::HandshakeEx(HandshakeExPdu { flags, .. })) => {
+                self.finish_handshake(Some(flags))
+            }
+            (RailState::WaitingForHandshake, _) => Err(PduError::new(
+                "RAIL",
+                PduErrorKind::Other {
+                    description: "received RAIL PDU before server handshake",
+                },
+            )),
+            (RailState::Established, RailPdu::Handshake(_) | RailPdu::HandshakeEx(_)) => Err(PduError::new(
+                "RAIL",
+                PduErrorKind::Other {
+                    description: "received duplicate RAIL server handshake",
+                },
+            )),
+            (RailState::Established, RailPdu::ExecuteResult(result)) => {
+                let Some(index) = self
+                    .pending_executes
+                    .iter()
+                    .position(|request| request.flags == result.flags && request.executable == result.executable)
+                else {
+                    return Err(PduError::new(
+                        "RAIL",
+                        PduErrorKind::Other {
+                            description: "received unmatched RAIL execute result",
+                        },
+                    ));
+                };
+                self.pending_executes.remove(index);
+                self.push_event(RailEvent::ExecuteResult(result))?;
+                Ok(Vec::new())
+            }
+            (
+                RailState::Established,
+                RailPdu::GetApplicationIdResponse(GetApplicationIdResponsePdu {
+                    window_id,
+                    application_id,
+                }),
+            ) => {
+                self.push_event(RailEvent::ApplicationId {
+                    window_id,
+                    application_id,
+                    process_id: None,
+                    process_image_name: None,
+                })?;
+                Ok(Vec::new())
+            }
+            (
+                RailState::Established,
+                RailPdu::GetApplicationIdResponseEx(GetApplicationIdResponseExPdu {
+                    window_id,
+                    application_id,
+                    process_id,
+                    process_image_name,
+                }),
+            ) => {
+                self.push_event(RailEvent::ApplicationId {
+                    window_id,
+                    application_id,
+                    process_id: Some(process_id),
+                    process_image_name: Some(process_image_name),
+                })?;
+                Ok(Vec::new())
+            }
+            (RailState::Established, RailPdu::ServerSystemParameters(parameters)) => {
+                self.push_event(RailEvent::Control(RailControlEvent::SystemParameters(parameters)))?;
+                Ok(Vec::new())
+            }
+            (RailState::Established, RailPdu::LanguageBarInfo(info)) => {
+                self.push_event(RailEvent::Control(RailControlEvent::LanguageBar(info)))?;
+                Ok(Vec::new())
+            }
+            (RailState::Established, RailPdu::CompartmentInfo(info)) => {
+                self.push_event(RailEvent::Control(RailControlEvent::Compartment(info)))?;
+                Ok(Vec::new())
+            }
+            (RailState::Established, RailPdu::ZOrderSync(sync)) => {
+                self.push_event(RailEvent::Control(RailControlEvent::ZOrderSync(sync)))?;
+                Ok(Vec::new())
+            }
+            (RailState::Established, RailPdu::Cloak(cloak)) => {
+                self.push_event(RailEvent::Control(RailControlEvent::Cloak(cloak)))?;
+                Ok(Vec::new())
+            }
+            (RailState::Established, RailPdu::PowerDisplayRequest(request)) => {
+                self.push_event(RailEvent::Control(RailControlEvent::PowerDisplayRequest(request)))?;
+                Ok(Vec::new())
+            }
+            (RailState::Established, _) => Ok(Vec::new()),
+        }
+    }
+}
+
diff --git a/crates/ironrdp-web/src/session.rs b/crates/ironrdp-web/src/session.rs
index 4a6ed6b5..b28e6543 100644
--- a/crates/ironrdp-web/src/session.rs
+++ b/crates/ironrdp-web/src/session.rs
@@ -13,6 +13,9 @@ use futures_util::{AsyncWriteExt as _, FutureExt as _, StreamExt as _, select};
 use gloo_net::websocket;
 use gloo_net::websocket::futures::WebSocket;
 use gloo_timers::future::IntervalStream;
+use ironrdp_rail::pdu as rail_pdu;
+
+use crate::rail::{RailClient, RailEvent};
 use iron_remote_desktop::{CursorStyle, DesktopSize, Extension, IronErrorKind};
 use ironrdp::cliprdr::CliprdrClient;
 use ironrdp::cliprdr::backend::ClipboardMessage;
@@ -97,6 +100,8 @@ struct SessionBuilderInner {
     enable_server_pointer: bool,
     legacy_graphics: bool,
     outbound_message_size_limit: Option<usize>,
+    /// Programa RemoteApp (RAIL) a abrir no lugar da área de trabalho.
+    remote_app: Option<rail_pdu::ExecutePdu>,
 }
 
 impl Default for SessionBuilderInner {
@@ -142,6 +147,7 @@ impl Default for SessionBuilderInner {
             enable_server_pointer: true,
             legacy_graphics: false,
             outbound_message_size_limit: None,
+            remote_app: None,
         }
     }
 }
@@ -260,6 +266,16 @@ impl iron_remote_desktop::SessionBuilder for SessionBuilder {
             |kdc_proxy_url: String| { self.0.borrow_mut().kdc_proxy_url = Some(kdc_proxy_url) };
             |display_control: bool| { self.0.borrow_mut().use_display_control = display_control };
             |enable_credssp: bool| { self.0.borrow_mut().enable_credssp = enable_credssp };
+            |remote_app: JsValue| {
+                let field = |name: &str| js_sys::Reflect::get(&remote_app, &JsValue::from_str(name)).ok().and_then(|v| v.as_string()).unwrap_or_default();
+                let program = field("program");
+                self.0.borrow_mut().remote_app = (!program.is_empty()).then(|| rail_pdu::ExecutePdu {
+                    flags: rail_pdu::ExecutePdu::EXPAND_WORKING_DIRECTORY | rail_pdu::ExecutePdu::EXPAND_ARGUMENTS,
+                    executable: program,
+                    working_directory: field("work_dir"),
+                    arguments: field("args"),
+                });
+            };
             |enable_server_pointer: bool| { self.0.borrow_mut().enable_server_pointer = enable_server_pointer };
             |legacy_graphics: bool| { self.0.borrow_mut().legacy_graphics = legacy_graphics };
             |outbound_message_size_limit: f64| {
@@ -434,6 +450,9 @@ impl iron_remote_desktop::SessionBuilder for SessionBuilder {
         let enable_credssp = self.0.borrow().enable_credssp;
         config.enable_credssp = enable_credssp;
 
+        let remote_app = self.0.borrow().remote_app.clone();
+        config.remote_application_mode = remote_app.is_some();
+
         let enable_server_pointer = self.0.borrow().enable_server_pointer;
         config.enable_server_pointer = enable_server_pointer;
 
@@ -524,6 +543,7 @@ impl iron_remote_desktop::SessionBuilder for SessionBuilder {
             computer_name: client_name.clone(),
             use_display_control,
             input_events_tx: input_events_tx.clone(),
+            remote_app,
         })
         .await?;
 
@@ -709,6 +729,7 @@ impl iron_remote_desktop::Session for Session {
         let activation_factory = connection_result.activation_factory;
 
         let mut active_stage = ActiveStageBuilder {
+            input_flags: connection_result.input_flags,
             static_channels: connection_result.static_channels,
             user_channel_id: connection_result.user_channel_id,
             io_channel_id: connection_result.io_channel_id,
@@ -947,6 +968,34 @@ impl iron_remote_desktop::Session for Session {
                 }
             };
 
+            // RemoteApp: quando o servidor conclui o handshake RAIL, envia o pedido de abrir o programa;
+            // se o servidor recusar, encerra a sessão com o motivo.
+            let rail_events = active_stage.get_svc_processor_mut::<RailClient>().map(RailClient::take_events).unwrap_or_default();
+            for event in rail_events {
+                match event {
+                    RailEvent::Handshake { .. } => {
+                        let messages = active_stage
+                            .get_svc_processor_mut::<RailClient>()
+                            .map(RailClient::release_queued_after_handshake)
+                            .transpose()
+                            .context("RemoteApp: release execute")?;
+                        if let Some(messages) = messages {
+                            let frame = active_stage.process_svc_processor_messages(messages)?;
+                            if !frame.is_empty() {
+                                self.writer_tx.unbounded_send(frame).context("Send frame to writer task")?;
+                            }
+                        }
+                    }
+                    RailEvent::ExecuteResult(result) if result.result != rail_pdu::ExecuteResult::Ok => {
+                        return Err(IronError::from(anyhow::anyhow!(
+                            "o servidor não abriu o RemoteApp {} ({:?}, código {:#x})",
+                            result.executable, result.result, result.raw_result
+                        )));
+                    }
+                    _ => {}
+                }
+            }
+
             // `process()` may have resized `image` to follow ResetGraphics in the same
             // frame. The canvas has to match *before* the GraphicsUpdate from that frame
             // is drawn.
@@ -1099,7 +1148,7 @@ impl iron_remote_desktop::Session for Session {
                             if let ConnectionActivationState::Finalized {
                                 desktop_size,
                                 share_id,
-                                input_flags: _,
+                                input_flags,
                                 enable_server_pointer,
                                 pointer_software_rendering,
                                 static_channel_chunk_size,
@@ -1129,6 +1178,7 @@ impl iron_remote_desktop::Session for Session {
                                 ) {
                                     return Err(IronError::from(anyhow::anyhow!("invalid static channel chunk size")));
                                 }
+                                active_stage.set_input_flags(input_flags);
                                 active_stage.set_window_support_level(window_support_level);
                                 break 'activation_seq;
                             }
@@ -1193,18 +1243,8 @@ impl iron_remote_desktop::Session for Session {
         caps_lock: bool,
         kana_lock: bool,
     ) -> Result<(), Self::Error> {
-        use ironrdp::pdu::input::fast_path::FastPathInput;
-
         let event = ironrdp::input::synchronize_event(scroll_lock, num_lock, caps_lock, kana_lock);
-        let fastpath_input = FastPathInput::single(event);
-
-        let frame = ironrdp::core::encode_vec(&fastpath_input).context("FastPathInput encoding")?;
-
-        self.writer_tx
-            .unbounded_send(frame)
-            .context("Send frame to writer task")?;
-
-        Ok(())
+        self.h_send_inputs(smallvec::smallvec![event])
     }
 
     fn shutdown(&self) -> Result<(), Self::Error> {
@@ -1675,6 +1715,8 @@ struct ConnectParams {
     use_display_control: bool,
     /// Lets the EGFX handler report server-driven desktop resizes to the event loop.
     input_events_tx: mpsc::UnboundedSender<RdpInputEvent>,
+    /// Programa RemoteApp: liga o canal RAIL e fica na fila até o handshake do servidor.
+    remote_app: Option<rail_pdu::ExecutePdu>,
 }
 
 fn default_printer_driver_name() -> String {
@@ -1726,6 +1768,7 @@ async fn connect(
         computer_name,
         use_display_control,
         input_events_tx,
+        remote_app,
     }: ConnectParams,
 ) -> Result<(connector::ConnectionResult, WebSocket), IronError> {
     let mut framed = ironrdp_futures::LocalFuturesFramed::new(ws);
@@ -1733,7 +1776,19 @@ async fn connect(
     // In web browser environments, we do not have an easy access to the local address of the socket.
     let dummy_client_addr = core::net::SocketAddr::V4(SocketAddrV4::new(Ipv4Addr::LOCALHOST, 33899));
 
+    let rail_client = match remote_app {
+        Some(execute) => {
+            let mut rail = RailClient::new(config.client_build, config.desktop_size.width, config.desktop_size.height);
+            // Antes do handshake só entra na fila (não gera mensagens).
+            let _ = rail.queue_execute(execute).context("queue RemoteApp launch")?;
+            Some(rail)
+        }
+        None => None,
+    };
     let mut connector = ClientConnector::new(config, dummy_client_addr);
+    if let Some(rail) = rail_client {
+        connector.attach_static_channel(rail);
+    }
 
     if let Some(clipboard_backend) = clipboard_backend {
         connector.attach_static_channel(CliprdrClient::new(Box::new(clipboard_backend)));
diff --git a/crates/ironrdp/examples/rdp_stress.rs b/crates/ironrdp/examples/rdp_stress.rs
index ee45471b..d9962f47 100644
--- a/crates/ironrdp/examples/rdp_stress.rs
+++ b/crates/ironrdp/examples/rdp_stress.rs
@@ -322,6 +322,7 @@ fn run(config: Config) -> anyhow::Result<u32> {
     };
 
     let mut stage = ActiveStageBuilder {
+        input_flags: connection_result.input_flags,
         static_channels: connection_result.static_channels,
         user_channel_id: connection_result.user_channel_id,
         io_channel_id: connection_result.io_channel_id,
diff --git a/crates/ironrdp/examples/screenshot.rs b/crates/ironrdp/examples/screenshot.rs
index 7073cc61..405965cd 100644
--- a/crates/ironrdp/examples/screenshot.rs
+++ b/crates/ironrdp/examples/screenshot.rs
@@ -352,6 +352,7 @@ fn active_stage(
     image: &mut DecodedImage,
 ) -> anyhow::Result<()> {
     let mut active_stage = ActiveStageBuilder {
+        input_flags: connection_result.input_flags,
         static_channels: connection_result.static_channels,
         user_channel_id: connection_result.user_channel_id,
         io_channel_id: connection_result.io_channel_id,
